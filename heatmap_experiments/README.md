### Legend for directories

- `aX`: X is number of attacks
- `tX`: X is number of sampled transitions
- `sX`: X is number of steps for which we collect heatmap statistics
- `dp`: error model
- `cX`: X is cost threshold. We only care about errors where cost is lower or same as threshold

